# Advanced Pipeline Optimization Dependencies
# Core numerical and scientific computing
numpy>=1.21.0
numba>=0.56.0

# System monitoring and process management
psutil>=5.8.0

# Asynchronous I/O and networking
aiofiles>=0.7.0
aiohttp>=3.8.0
uvloop>=0.16.0

# Concurrent programming utilities (built-in since Python 3.2)
# concurrent-futures>=3.1.1  # Not needed for Python 3.2+

# Memory management and weak references (built-in)
# weakref2>=1.0.0  # Not needed - weakref is built-in

# Optional: GPU acceleration (CUDA)
# numba-cuda>=0.56.0  # Uncomment if CUDA is available

# Optional: Additional performance monitoring
# memory-profiler>=0.60.0  # For detailed memory profiling
# line-profiler>=3.4.0     # For line-by-line profiling

# Development and testing dependencies
pytest>=6.2.0
pytest-asyncio>=0.15.0
pytest-benchmark>=3.4.0

# Documentation
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0 